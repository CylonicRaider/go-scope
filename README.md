# scope

Package `scope` provides context objects for the sharing of scope across
goroutines. The API is a strict superset of that of `context.Context`.

See [doc.go](`scope/doc.go`) for more details.
